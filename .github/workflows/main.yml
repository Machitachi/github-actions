name: Bem-vindo a aula 01

on: [push, pull_request]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Show a message
        run: echo 'Bem-vindo'
      - name: Multiple commands
        run: |
          node -v
          npm -v
          